logging{
   loglevel = DEBUG
   pattern = "%d{HH:mm:ss.SSS} %p [%thread][%F] %m%n"
}

keystore {
   name = my.keystore
   loggingStep = 10000
}

generator {
   length = 3
   storage = /media/legioner/5E4C53C25A90493F/Projects/storage.ser
   isForced = true
}